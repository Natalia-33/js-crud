{{! 
Це код container файлу для index сторінки, 
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/"
 }}
<main class='main'>
  {{#with data}}
    <div class='main__content'>
      <div class='search'>
        <h1 class='title title--center'>Моя бібіліотека</h1>

        <div class='search__button'>
          <form method='POST' action='/spotify-search'>
            <button type='submit'>
              <img
                src='/svg/spotify-search.svg'
                height='47px'
              />

            </button>
          </form>

          <form method='GET' action='/spotify-choose'>
            <button type='submit'>
              <img
                src='/svg/add_button.svg'
                height='42px'
              />
            </button>

          </form>
        </div>
      </div>
    </div>

    <ul class='playlist__list'>
      {{#each list}}
        {{!-- {{#if isEmpty}}
          <div class='playlist__name'>Ще не створено жодного
            плейліста. Додайте, будь-ласка перший!</div>
        {{/if}} --}}
        <li class='playlist'>
          <img
            src='{{image}}'
            alt='{{name}}'
            height='92px'
          />
          <div class='playlist__content'>
            <strong class='playlist__name'>{{name}}</strong>
            <span class='playlist__amount'>Кількість пісень:{{amount}}</span>
            <span disabled>{{playListId}}</span>
          </div>
        </li>
      {{/each}}

    </ul>

  {{/with}}
</main>
{{! 
    Цей код містить лише те, що буде виводитись в <body>
    Адже наш layout/default.hbs має вже структуру HTML
}}